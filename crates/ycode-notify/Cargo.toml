[package]
name = "ycode-notify"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Tiny helper invoked by agent CLI hooks (Claude Code Stop, Codex notify) to relay completion events to the running ycode app over a Unix domain socket."

[[bin]]
name = "ycode-notify"
path = "src/main.rs"

[dependencies]
serde_json.workspace = true
